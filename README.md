# Scrapbook Gift Site

This project is a static site that works with GitHub Pages.

## Files

- `index.html` - page structure and placeholder story sections
- `style.css` - scrapbook styling, collage background, spacing, and animations
- `script.js` - scroll reveals and background parallax

## GitHub Pages Setup

1. Create a new GitHub repository.
2. Upload the contents of this folder to the repository root.
3. In GitHub, open `Settings` -> `Pages`.
4. Under `Build and deployment`, choose `Deploy from a branch`.
5. Select the `main` branch and the `/ (root)` folder.
6. Save and wait for GitHub Pages to publish the site.

The site entry point is `index.html`, so no build step is required.

## Recommended Workflow

For our iteration loop, the simplest flow is:

1. I update the site files here.
2. You upload or sync the changed files to the GitHub repo.
3. You refresh the GitHub Pages URL to see the latest version.
4. We adjust layout, text, photos, and motion based on what you see.

## Photos Later

When you're ready, we can add:

- an `images/` folder for real photos
- centered featured photos between text moments
- side photos with white disposable-camera borders
- a blurred scrolling background collage built from your uploaded images
